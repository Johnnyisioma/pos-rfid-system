{
  "name": "pos-rfid-android",
  "version": "5.0.0",
  "private": true,
  "description": "Android shell for the POS + RFID system, with a native bridge to the handheld's UHF reader",
  "scripts": {
    "sync": "node scripts/stage-web.mjs && cap sync android",
    "open": "cap open android",
    "apk": "node scripts/stage-web.mjs && cap sync android && cd android && ./gradlew assembleDebug",
    "apk:release": "node scripts/stage-web.mjs && cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.0.1",
    "@capacitor/core": "^8.5.2",
    "@capacitor/haptics": "^8.0.1",
    "@capacitor/keyboard": "^8.0.1",
    "@capacitor/preferences": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
